# Prettier formats both markdown (`lint:md` / `format:md` — see package.json) and
# JavaScript (`format:js` / `format:js:fix`) in this project. `.prettierrc` scopes
# JS-specific options (tabWidth: 4, singleQuote: true) to JS/TS via an `overrides`
# block, so markdown keeps prettier's own defaults and its output is unaffected by
# the JS settings. See the Markdown and JavaScript sections under Code Standards in
# CLAUDE.md for the full rationale, including why this used to be markdown-only.

# Build output and dependencies
dist/
node_modules/
storybook-static/
.yarn/
.pnp.*

# Session scratch from agent-driven development — untracked working notes, not
# project documentation. (`docs/superpowers/` IS tracked and IS formatted.)
.superpowers/
